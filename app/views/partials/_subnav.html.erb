<nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom">
    <div class="container-fluid">
        
        <button class="btn btn-primary text-dark invisible" id="menu-toggle"><i class="fas fa-bars"></i></button>

        <!--
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        -->
        <%if current_user.is_normal_user?%>
        <div class="" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto mr-3 mt-2 mt-lg-0">
                <!--
                <li class="nav-item active">
                    <a class="nav-link text-dark" href="#">Inicio</a>
                </li>-->
                <li class="nav-item dropdown">
                    <a class="nav-link text-dark dropdown-toggle text-capitalize" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <%=current_user.name%> <%=current_user.last_name%> <img src="/img/paises/<%=current_user.country.capitalize%>.svg" alt="<%=current_user.country%>" title="<%=current_user.country%>" class="avatar">
                    </a>

                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                        <td class="align-middle"><a class="dropdown-item" href="/users/edit">Mi Perfil <i class="fas fa-user-circle" style="font-size:30px"></i></a></td>
                    </div>
                </li>
            </ul>
        </div>
        <%end%>
    </div>
</nav>


