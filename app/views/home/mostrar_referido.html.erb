<%=render "partials/btn-back", link: :back%>
<div class="row justify-content-center mt-3 mb-5">
    <div class="col-11 col-lg-7 p-3 fondo" style="border-radius:12px;">
        <div class="tab-pane fade show active field" id="home" role="tabpanel" aria-labelledby="home-tab">

            <%=render 'partials/info-usuarios', usuario: @usuario_a_referir%>

            <a href="/confirm_referido?notify=<%=params[:notify]%>" class="btn btn-info mb-2" onclick="show_preloader()">Sí, si lo recomiendo</a>
            <a href="/rechazar_referido?notify=<%=params[:notify]%>" class="d-inline-block float-right btn btn-danger" onclick="show_preloader()">No, no lo recomiendo</a>
        </div>
    </div>
</div>
